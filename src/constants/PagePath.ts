export enum PAGE_PATH {
    login = '/',
    signUp = '/sign-up',
    changePassword = '/change-password',
    messenger = '/messenger',
    notFound = '/not-found',
    internalServerError = '/internal-server-error',
}
